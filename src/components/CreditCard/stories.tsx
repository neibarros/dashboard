import { Story, Meta } from '@storybook/react/types-6-0'
import CreditCard from '.'

export default {
  title: 'CreditCard',
  component: CreditCard
} as Meta

export const Default: Story = () => <CreditCard />
