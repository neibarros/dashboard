import { Story, Meta } from '@storybook/react/types-6-0'
import ColChart from '.'

export default {
  title: 'ColChart',
  component: ColChart
} as Meta

export const Default: Story = () => <ColChart />
